import SwiftUI

// SidebarView remplacée par DashboardView
// Fichier conservé pour compatibilité XcodeGen

struct SidebarView: View {
    @Binding var selectedModule: Module?

    var body: some View {
        EmptyView()
    }
}
